public class Flower {//kc
    /* 
    int petalCount = 0;
String s = new String(original:"null");
//Contructor 1
    Flower(int petals) {
        petalCount = petals;
    }

    // Constructor 2
    Flower(String ss) {
        S = SS;
    }

    // Constructor 3
    Flower(String s, int petals) {
        this(petals);
        // ! this(s); // can't do it twice
        this.s = s;
    }

    // Default Constructor: NO argument
Flower() {
this(s: "hello", petals: 47);
}// this here is s and petals,
     // question: order?x

     */
}